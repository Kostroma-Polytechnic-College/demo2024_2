<Page x:Class="WpfApp2.Pages.Test"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp2.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Test" Loaded="Page_Loaded">

    <Grid>
        <DockPanel>
            <StackPanel DockPanel.Dock="Top" Orientation="Horizontal">
                <Button  Content="Выход" HorizontalAlignment="Left" VerticalAlignment="Top"  Click="Button_Click"/>
                <TextBox x:Name="filter" TextWrapping="Wrap" Text="" Width="300" TextChanged="filter_TextChanged"/>
                <Button x:Name="edit" Content="Изменить" Click="edit_Click"/>
                <Button x:Name="delete" Content="Удалить" Click="delete_Click"/>
            </StackPanel>
            <DataGrid x:Name="table"/>
        </DockPanel>

    </Grid>
</Page>
